# 1단계 - 칸반 보드 태스크(카드)

## 칸반 보드 요소

### 1. 컨테이너

- [x] 보드의 각 요소를 배치할 컨테이너를 Box로 선언

### 2. 제목

- [x] 보드의 제목을 표시할 텍스트 컴포넌트
- [x] 컨테이너의 가로 길이보다 표시할 문자열이 길 경우 문자열의 뒷부분을 말줄임표로 대치
- [x] 줄넘김을 허용하지 않음

### 3. 중간 내용

- [x] 보드에 중간 내용을 표시할 텍스트 컴포넌트
- [x] 컨테이너의 가로 길이보다 표시할 문자열이 길 경우 문자열의 뒷부분을 말줄임표로 대치
- [x] 줄넘김은 최대 2줄까지 허용

### 4. 태그

- [x] 태그를 표시할 컨테이너
    - [x] flowRow로 선언
- [x] 각 태그의 내용을 표시할 텍스트 컴포넌트
    - [x] 최대 글자수는 5자로 제한
- [x] 태그의 최대 개수는 5개로 제한

### 5. 작성자

- [x] 아이콘과 사용자 이름을 표시할 컨테이너
- [x] 작성자의 이름이 컨테이너의 가로 길이보다 길 경우 문자열의 뒷부분을 말줄임표로 대치
- [x] 줄넘김을 허용하지 않음

# 함수 설명

## KanbanBoardMax

칸반 보드의 각 요소가 최대일 경우를 표현한 함수

## KanbanBoardCase1

칸반 보드의 각 요소가 전부 존재하는 상태를 표현한 함수

## KanbanBoardCase2

칸반 보드의 요소 중 중간 내용이 존재하지 않는 상태를 표현한 함수

## KanbanBoardCase3

칸반 보드의 요소 중 태그가 존재하지 않는 상태를 표현한 함수

## KanbanBoardCase4

칸반 보드의 요소 중 중간 내용과 태그가 존재하지 않는 상태를 표현한 함수

---

# 1차 피드백 반영

## 1. 변경사항
- [x] 프리뷰 렌더링을 위한 함수가 아닌 실제 카드를 생성하기 위해 각 요소를 함수로 추출
- [x] 여러장의 카드를 출력할 보드 : CardBoardView 함수
- [x] 각 요소를 통합할 카드 몸체 : KanbanBoardCard 함수
- [x] 제목을 출력할 컴포넌트 : CardTitle 함수
- [x] 본문을 출력할 컴포넌트 : CardContent 함수
- [x] 태그를 출력할 컴포넌트 : CardTags 함수
- [x] 작성자 정보를 출력할 컴포넌트 : CardWriter 함수
- [x] 카드에 표시할 문자열들을 저장하기 위한 data class : CardData.class
- [x] 색상 팔레트, 예제 문자열 등을 저장할 상수 필드 : MyConst.kt

## 2. 질문 사항
### 2-1. CardWriter 함수에서 icon 컴포넌트를 분리해야 하는지
다른 컴포넌트는 함수로 분해했는데 아이콘만 작성자 이름 컴포넌트와 같은 함수에서 생성합니다. 이를 분리시키는게 좋을까요?
### 2-2. App.kt, CardData.class, MyConst.kt의 디렉토리 분리
3개의 파일이 같은 디렉터리에 위치해있는데 패키지를 나누는게 좋을까요? 나눠야 한다면 어떤 구조를 적용해서 패키지를 나누는게 좋을까요?

---

# 2단계 - 칸반 보드 태스크(리팩터링)
## 수정사항 1. 작성자 컴포넌트 분리
- [x] 기존 Icon과 writer 컴포넌트를 한 컴포저블에서 생성하던 코드를 Icon 생성 컴포저블과 writer 생성 컴포저블로 분해

## 수정사항 2. CardData 생성 방식 변경
- [x] 기존 기본 생성자를 통해 객체를 생성하는 방식에서 companion object에 createCard 함수를 추가
- [x] createCard 함수에 입력값 검증 추가
### 수정사항2 질문사항
1. init 키워드를 사용한 커스텀 생성자를 정의하여 입력 데이터를 검증하는 것과 현재 방식 중 어떤 것이 더 효율적일까요?

## 수정사항 3. 테스트 수행을 위한 컴포저블 testTag 추가
- [x] 입력 데이터에 따른 컴포넌트 생성 여부 테스트를 위한 testTag 추가

---

# 2단계 - 칸반 보드 태스크(테스트 코드)

# 1. 단위 테스트 (CardDataTest.kt)
### [x] case1. 제목 미입력 오류 테스트
－ 제목 데이터 미입력시 IllegalArgumentException이 발생하는지 검증

### [x] case2. 작성자 정보 미입력 오류 테스트
- 작성자 이름 미입력시 IllegalArgumentException이 발생하는지 검증

### [x] case3. 태그 개수(5개 이상) 검증 테스트
- 입력된 태그의 개수가 5개 제한을 지키지 않았는지 검증
- 제한이 지켜지지 않았을 경우 IllegalArgumentException이 발생하는지 검증

### [x] case4. 태그 글자수(5글자 이상) 검증 테스트
- 입력된 각 태그의 글자수가 5글자 이하인지 검증
- 5글자 이상인 태그가 존재할 경우 IllegalArgumentException이 발생하는지 검증

### [x] case5. 모든 정보가 정상적으로 입력되었을 경우
- 모든 정보가 입력되었을 때 객체가 정상적으로 생성되는지 검증

### [x] case6. 본문을 입력하지 않았을 경우
- 본문이 입력되지 않은 상황에서 객체가 정상적으로 생성되는지 검증

### [x] case7. 태그를 입력하지 않았을 경우
- 태그가 입력되지 않은 상황에서 객체가 정상적으로 생성되는지 검증

### [x] case8. 본문, 태그 둘 다 입력되지 않았을 경우
- 제목, 작성자 정보 만있어도 객체가 정상적으로 생성되는지 검증

# 2. UI 테스트 (KanbanBoardUITest.kt)
### [x] case1. 모든 정보가 입력된 경우
- 제목, 본문, 태그, 작성자 컴포넌트 모두 생성되는지 검증

### [x] case2. 본문이 생략된 경우
- 본문을 제외한 나머지 컴포넌트만 생성되는지 검증

### [x] case3. 태그가 생략된 경우
- 태그를 제외한 나머지 컴포넌트만 생성되는지 검증

### [x] case4. 본문, 태그가 생략된 경우
- 본문, 태그 컴포넌트를 제외한 나머지 컴포넌트만 생성되는지 검증

### [ ] case5. 각 컴포넌트의 내용이 너무 길 경우 elilpsis가 제대로 동작하는지 검증
- 컴포넌트의 가로 길이보다 내용이 긴 경우 글줄임표가 출력되는지 검증
- 구현 해보려 자료 조사를 해봤지만 지금 단계에선 이해하기 어려워 공부를 더 해봐야 할 것 같음 

# 2단계 - 칸반 보드 태스크 피드백 반영

### [x] 관심사에 따른 Const 분리

### [x] checkTagLength 함수 이름에 Prefix 관례 적용
```kotlin
isXxx
hasXxx
canXxx
shouldXxx
```
- [x] 함수 내부 구현도 코틀린스럽게 바꿔보기
- checkTagLength -> isLongerThenFive

### [x] Preview 인자를 통한 프리뷰 케이스 적용해보기
- light mode와 dark mode에 대한 Preview 선언
- desktop 환경 화면 비율과 phone 혼경 화면 비율에 대한 Preview 선언

### [x] 컴포넌트 재사용을 위한 파라미터
- 반복되는 구조를 함수로 추출하여 Modifier를 인자로 전달하여 재사용성을 높일 수 있음
- 추출한 함수에 Modifier를 전달하고 Box 안에 배치할 컴포저블을 @Composable BoxScope.()를 통해 Text 컴포저블을 전달해보고 싶었지만 아직 공부가 더 필요할 것 같습니다... 

### [x] 외부에서 사용하지 않는 컴포저블 함수의 공개범위 수정

### [x] REAME.md 파일 최신화

### [x] 테스트 시나리오 추가
- 현재 실패 상황에 대한 테스트만 있음
- 정상적인 입력값을 통한 생성 성공 케이스 추가

### [x] 테스트 코드의 파일명과 테스트 대상 파일명을 일치시키기
- EX) CardDataTest

### [ ] content의 내용이 null일 때 컴포저블이 그려지지 않을 것이라 예상하기 어려움
