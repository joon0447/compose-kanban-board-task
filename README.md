# 칸반 보드 태스크(카드)

## 요구 사항
### 기능 요구 사항
- 비즈니스 로직과 UI 로직을 분리한다.

### 프로그래밍 요구 사항
- 단위 테스트만으로도 충분한 로직과, UI 테스트가 필요한 영역을 구분한다.
- 핵심 비즈니스 로직을 가지는 객체를 분리해 단위 테스트를 진행한다.
- Compose UI Testing을 활용하여 1단계의 기능 요구 사항을 테스트한다.

## 구현할 기능 / 테스트 목록 (2단계)
### UI - 비즈니스 로직 분리
- [X] 로직 분리를 위한 클래스 생성
- [x] 제목 누락 시 생성 불가 로직 추가
- [x] 본문 누락 시 렌더링 제외 로직 분리
- [x] 태그 개수 로직 분리
- [x] 태그 텍스트 길이 제한 로직 분리
- [x] 공백 처리 로직 추가

### 비즈니스 로직 분라에 따른 UI 수정
- [x] 파라미터 명 수정
- [x] UI 내 비즈니스 로직 제거
- [x] 주석 수정

### Unit 테스트
- [x] 제목 유효 여부
- [x] 본문 유무
- [x] 태그 개수 제한
- [x] 태그 길이 제한

### UI 테스트
- [x] 제목, 본문, 태그, 계정 정보의 화면 표출 여부
- [x] 본문 누락 시 렌더링 여부
- [x] 태그 개수에 따른 출력
- [x] 태그 글자수에 따른 출력

## 리팩토링
### 2단계
- 프로덕션
  - [x] API Guidelines for components in Jetpack Compose에 따른 파라미터 순서 변경
  - [x] 피드백에 따른 KDoc 주석 수정 (간결, 요약)
  - [x] private composable 프리뷰 코드 작성
  - [x] modifier 재사용성 피드백에 따른 로직 수정
  - [x] 피드백에 따른 기능 요구사항 재정의 및 KanbanBoardCardData.create() 메서드 내 로직 수정
  - [x] API Guidelines for components in Jetpack Compose에 따른 파라미터 순서 변경 작업 중 누락한 composable 수정
- 테스트
  - [x] 프로덕션 패키지 구조와 테스트 패키지 구조 얼라인 및 파일 분리
  - [x] 모호한 테스트 함수명 수정
  - [x] KanbanBoardCardData.create()의 비즈니스 로직 테스트 / UI 테스트 모호성 수정
  - [x] KanbanBoardCardData.creaete() 로직 변경에 따른 테스트 코드 수정
    - [x] 태그 개수 / 글자수 제한을 어길 시 assertFail로 로직 변경