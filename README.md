# 🚀 칸반 보드 태스크

안드로이드 8기 레벨 1 미션, 칸반 보드용 태스크 카드 컴포넌트를 구현하고 관리하는 프로젝트입니다.

---

## 2단계: 리팩터링 및 테스트

2단계에서는 1단계에서 구현한 UI 컴포넌트의 관심사를 분리하고 안정적으로 검증하기 위한 테스트 환경을 구축하는 데 집중합니다.

### 설계 고민과 해결 방향

1단계 리뷰 과정에서 "UI 컴포넌트가 예외를 던지거나, 데이터를 직접 제한하는 것이 과연 좋은 설계일까?"라는 고민을 하게 되었습니다. 만약 외부에서 정제되지 않은 데이터가 넘어와 UI 컴포넌트에서 크래시가 발생한다면 최악의 UX가 발생하게 됩니다.

따라서 이번 2단계에서는 도메인 계층과 UI 계층의 역할을 명확히 분리하고자 합니다.

- "제목은 비어있을 수 없다", "태그는 5개를 초과할 수 없다"와 같은 핵심 비즈니스 규칙을 강제하고 검증하는 `Task` 객체를 도입합니다. 특정 플랫폼에 의존하지 않는 순수 코틀린 코드로 작성하여 단위 테스트로 검증합니다.
- 도메인 객체로부터 이미 정제되고 검증된 데이터를 전달받아 컴포넌트는 오직 주어진 상태를 화면에 표현하는 역할에만 집중하도록 리팩터링합니다.
    - [추가 고민] 초기에는 컴포넌트에 도메인 모델을 그대로 주입하려 했지만 특정 도메인 객체와 UI 컴포넌트를 강하게 결합시켜 컴포넌트의 재사용성을 떨어뜨린다고 판단했습니다. 
    따라서 `KanbanCard`의 기존 파라미터 구조를 그대로 유지하고 호출부에서 도메인 모델의 상태를 주입하는 방식으로 방향을 수정했습니다.

### 구현 기능 목록

### 1. 비즈니스 로직 분리 및 도메인 모델 설계

- [x]  `KanbanTask` 도메인 모델 생성
- [x]  제목 상태 관리 및 검증 로직 구현
    - [x]  제목이 비어 있거나 공백만 있는 경우 객체 생성 방지
- [x]  태그 관리 로직 분리
    - [x]  UI에 노출될 최대 5개의 태그만 제공하도록 캡슐화
- [x]  본문 내용, 담당자 정보 등 기타 데이터 상태 정의

### 2. 단위 테스트

- [x]  `KanbanTask` 객체의 비즈니스 규칙 검증 (정상 및 예외 케이스)
- [x]  텍스트 빈 값 검증, 태그 개수 제한 등 핵심 로직 단위 테스트 작성

### 3. 컴포넌트 리팩터링 및 UI 테스트

- [x]  `KanbanCard` 호출부 리팩터링: 상위에서 도메인 모델을 생성하고 분해된 상태를 컴포넌트에 주입하도록 `@Preview` 코드 수정
- [x]  Compose UI Testing 환경 구성 및 의존성 추가
- [x]  UI 테스트 시나리오 작성 및 동작 검증
    - [x]  시나리오 1: 모든 데이터(제목, 본문, 태그, 담당자)가 존재하는 경우의 카드 노출 확인
    - [x]  시나리오 2: 선택적 데이터(본문, 태그 등)가 없는 경우의 레이아웃 적응 확인


---
## 1단계: UI 컴포넌트 구현

1. 카드 프레임

- 요구사항 분석: 배경이 하얀색이고 곡률과 외곽선이 존재하는 카드의 뼈대입니다.
- UI 명세:
    - 배경색: 하얀색
    - 테두리: 1px, 색상 gray
    - 모서리 곡률: 10px
    - 내부 여백: 17px
    - 내부 컴포넌트 간 간격: 12px

### 2. 카드 제목

- 요구사항 분석: 카드의 제목을 표시하며, 길이가 길어 영역을 벗어나면 말줄임 처리를 합니다.
- UI 명세:
    - 텍스트 속성: 폰트 사이즈 16, 굵기 Bold
    - 줄 수 제한: 최대 1줄
    - 초과 처리: 말줄임

### 3. 카드 본문

- 요구사항 분석: 카드의 상세 본문을 표시합니다. 내용이 없을 경우 생략 가능하며, 길이가 길면 최대 2줄까지만 노출하고 말줄임 처리합니다.
- UI 명세:
    - 상태 처리: 데이터가 없으면 렌더링 생략
    - 텍스트 속성: 폰트 사이즈 14px, 색상 DarkGray
    - 줄 수 제한: 최대 2줄
    - 초과 처리: 말줄임

### 4. 태그 영역

- 요구사항 분석: 카드와 연관된 태그들을 표시합니다. 태그 당 글자 수는 최대 5자로 제한합니다. 태그는 최대 5개까지만 노출하며, 데이터가 없을 경우 렌더링을 생략합니다.
- UI 명세:
    - 상태 처리: 데이터가 없으면 렌더링 생략
    - 데이터 제한: 최대 5개 노출
    - 스타일: 배경색 gray, 텍스트 색상 Black
    - 레이아웃 구조: 공간에 따라 자연스럽게 줄바꿈이 되는 레이아웃
    - 레이아웃 간격: 아이템 간 가로 간격 8px, 세로 간격 4px

### 5. 프로필 정보

- 요구사항 분석: 카드 하단에 담당자의 정보인 프로필 사진, 크루 네임을 표시하며, 본문 영역과 구분하기 위한 선이 존재합니다.
- UI 명세:
    - 구분선: 영역 상단 테두리 1px, 색상 lightGray
    - 프로필 이미지: 크기 24px
    - 크루 네임 텍스트:
        - 폰트 사이즈 14px
        - 최대 1줄, 초과 시 말줄임 처리
    - 레이아웃 간격: 프로필 이미지와 크루 네임 사이의 간격 8px
